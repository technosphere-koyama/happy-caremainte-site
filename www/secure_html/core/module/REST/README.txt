/*
 * REST
 * 
 * 基幹システムと通信(REST、APEXAPI)するためのモジュール
 *
 * 機能としては以下
 *
 *	次に、そのcodeを持ってアクセストークン・リフレッシュトークンを取得する
 *	※SalesforceOAuth.incのauth_action()を実行すれば取得できる
 *	　但し、最初しか必要ないので処理が繋がってはいない
 *	　必要になったら繋いで画面から実行すること
 *	※SF管理画面の設定でリフレッシュトークンを出力するように設定してもらわないと得られないので気をつけること
 *
 * ・GET、POSTメソッドによりAPIなど実行する処理
 *	上記にて、一度アクセストークン・リフレッシュトークンを取得して以降は
 *	アクセストークンを用いて
 *	SalesforceRest.incのrest_exec_method_get()、rest_exec_method_post()から
 *	通信処理を行う
 *	アクセストークンの期限が切れたというエラーが返ってきたらリフレッシュトークンによりアクセストークンをとりなおす
 *	以降はこの繰り返しにて認証を通す
 */