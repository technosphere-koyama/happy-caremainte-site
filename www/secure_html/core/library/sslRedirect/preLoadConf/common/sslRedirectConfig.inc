<?php

/*
 * 時限設定
 * STARTからENDまでの間制限が有効となる
 * ※0を指定すると、制限なくずっと有効となる
 * STARTを0、ENDは時間指定とすると、
 * 設定直後から有効となり、ENDが来たら無効となる
 * 逆に、STARTは時間指定して、ENDは0とすると、
 * START指定までは無効で、START指定まできたら、以降はずっと有効となる
 * 時間指定を行う場合は、strtotime()にて行うこと
 * 設定文字列については
 * 　<年>-<月>-<日> <時>:<分>:<秒>
 * 　各パラメータは全て2桁にすること
 * 　※年も2桁で
 */
$config["SSL_RESTRICT_TIMER"] = array(
	"START" => 0,					//開始(開始時刻設定無し)
	//"START" => strtotime('15-10-31 00:00:00'),	//開始
	"END" => 0,					//終了(終了時刻設定無し)
	//"END" => strtotime('15-10-07 19:05:10')	//終了
);

/*
 * 20160208 add
 * SSLリストリクトタイプ
 * 80→443以外に、443→80も必要になったため、
 * タイプを設定し、選んで挙動させるようにする
 */
define("SSL_RESTRICT_TYPE_NOT_SSL", 80);		//HTTPへリダイレクト
define("SSL_RESTRICT_TYPE_SSL", 443);			//HTTPSへリダイレクト

?>
