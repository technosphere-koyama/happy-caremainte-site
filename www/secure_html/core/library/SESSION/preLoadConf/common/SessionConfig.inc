<?php

$config["SESSION"] = array(
	/*
	 * セッションの名前(セッションIDを保持するクッキーの名前として使われる。)
	 * ※デフォルトは PHPSESSID
	 * ※システムによってユニークになるように変更すること！
	 */
	"NAME" => 'kh',		

	/*
	 * session_cache_limiter 用設定値
	 * 
	 * これは、セッションを使ったページをキャッシュ出来るかどうかの指定
	 * デフォルトはnocache
	 * 
	 * クライアントに送信されるキャッシュ制御用のHTTPヘッダを制御する。
	 * これらのヘッダは、ページの内容をクライアントやプロキシがキャッシュする規則を定義する。
	 * 
	 * 設定値
	 *	・public
	 *		キャッシュを許可する。
	 *	・private
	 *		プロキシがキャッシュすることは許可しないが、クライアントがキャッシュすることは許可される。
	 *	・private_no_expire
	 *		privateモードでは、Expireヘッダがクライアントに送信される。
	 *		これは、Mozilla のようないくつかのブラウザを混乱させてしまうらしい。
	 *		これを避けるには、 private_no_expireモードを使用する必要がある。
	 *		private_no_expireでは、Expireヘッダはクライアントに送信されなくなる。
	 *	・nocache
	 *		クライアント/プロキシのキャッシュは無効になる。
	 *		※戻るボタンをクリックした際にもキャッシュさせないため、再処理を行う
	 *		　これのお陰で、エラーにならずにちゃんと戻れるようになる
	 *		　なので、基本的にはこれにしておくことをお勧めする
	 */
	"CACHE_LIMITER" => 'nocache',

	/*
	 * キャッシュ有効期限(分)
	 * これは、CACHE_LIMITERでprivate, publicを指定した場合の有効期限
	 * nocacheを指定している場合は関係ない
	 * ※デフォルトは180分
	 */
	"CACHE_EXPIRE" => (60 * 24 * 365),

	/*
	 * セッションファイル保存場所
	 * セッションは、ドメインやサイト毎に区別を行うわけではないので、
	 * ここで意図的に独自ディレクトリを作成して区別しておく
	 * ※システム自体のトップディレトリに「session」ディレクトリを作成し、そこに保存する
	 * ※herokuでは、GITプッシュじゃない形で追加されたものは削除されてしまうので、
	 * 　herokuの場合はデフォルトにしておくこと(SAVE_PATHを空に)
	 * 　デフォルトだと大丈夫だという保証もないが。。。
	 */
	//"SAVE_PATH" => '',
	"SAVE_PATH" => DIR_TMP . DS . 'session',

	/*
	 * session_regenerate_id()の連続実行によるセッションデータ消失対策用に
	 * ランダム実行を行う
	 * ここでの指定値分の1で実行することになる
	 */
	"REGENERATE_ID_RANDAM_TIME" => 10,

	/*
	 * ランダムチェックをスルーした後、
	 * 前回更新から最低何秒は更新しないという制約を行う
	 * その際の秒数指定
	 */
	"REGENERATE_ID_WAIT_TIME" => 1,

	/*
	 * セッション有効期限(秒)
	 * session.gc_maxlifetime
	 * ※サーバ側の期限
	 * ※デフォルトは1440秒
	 */
	"MAX_LIFE_TIME" => (60 * 60 * 24 * 365),
);

$config["COOKIE"] = array(
	/*
	 * クッキー有効期限(秒)
	 * ※デフォルトは0 (0にするとブラウザ閉じるまでとなる)
	 * 3600と指定すると、一旦ブラウザを閉じても1時間は有効になる
	 * ※ブラウザを閉じても2週間はセッションを保持してと言われると、
	 * 　ここも0ではなく、数値指定の方が良いと思われる
	 */
	"LIFE_TIME" => (60 * 60 * 24 * 365),

	/*
	 * クッキーの対象範囲指定
	 * ※デフォルトは「/」
	 */
	"TARGET_PATH" => '/'
);

?>
