<?php

$config["SESSION"] += array(
	/*
	 * セッション有効期限(秒)
	 * ※システムで利用する独自の期限
	 * ※期限を過ぎていたらセッションの中身をクリア
	 * ※アクセスする毎に期限を更新するので、
	 * 　触らなくなって指定時間が経てばクリアとなる
	 */
	"ORIGINAL_EXPIRE" => (60 * 60 * 24 * 365)
	//テストのため3分にする
	//"ORIGINAL_EXPIRE" => (60 * 3)
);

?>
